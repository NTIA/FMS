name: gwems
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - jupyter
  - IPython
  - numpy
  - scipy
  - pip
  - pip:
    - pre-commit # Also maybe delete this later?